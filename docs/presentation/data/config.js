/* ============================================================================
 *  STRIX 발표자료 — 편집용 설정 파일
 *
 *  🚨 이 파일은 지금 **아무 데서도 읽지 않습니다** (2026-07-31 실측).
 *     §14 에서 레이아웃 모델을 바꾸면서 발표 내용의 원천이 `data/deck.js` 로 옮겨갔고,
 *     `index.html` 은 이 파일을 로드조차 하지 않습니다. `window.__DECK_CONFIG__` 를 읽는 곳은
 *     옛 P1 덱(`_archive/index_p1_flow.html`) 하나뿐입니다.
 *     → **여기를 고쳐도 화면은 안 바뀝니다.** 표지·연락처를 고치려면
 *        덱을 열고 E 키(편집 모드)로 그 자리를 직접 고치거나 `data/deck.js` 를 고치세요.
 *     아래 값들은 옛 덱과 어긋나지 않게 참고용으로만 최신 상태를 유지합니다.
 *
 *  ⚠ 이 파일은 자동 생성 대상이 아닙니다. 빌드를 다시 돌려도 덮어쓰지 않습니다.
 * ========================================================================== */

window.__DECK_CONFIG__ = {

  /* ── 표지(1장) ────────────────────────────────────────────────────────── */
  title:     'STRIX',
  // 표지 큰 제목

  subtitle:  'OpenSees 기반 구조해석 · 설계 통합 플랫폼',
  // 표지 부제 — 한 줄로 제품을 설명하는 문장

  tagline:   '연구급 정확도를 실무의 손에',
  // 표지 맨 위 짧은 문구 (없애려면 '' 로)

  /* ── 발표 정보 ───────────────────────────────────────────────────────── */
  event:     '한국건축구조기술사회',
  // 발표처 이름

  date:      '2026. 8.',
  // 발표 일자

  presenter: '',
  // 발표자 이름 — 비워두면 표지에서 이 줄이 사라집니다

  role:      '',
  // 발표자 직함 (예: '대표이사') — 비워두면 안 나옵니다

  org:       'LPK SOFT Co., Ltd.',
  // 소속 회사명

  version:   '',
  // 표지 우하단에 표시할 앱 버전

  /* ── 맺음 슬라이드 · 연락처 ──────────────────────────────────────────── */
  contact: {
    web:   'strix-build.com',   // 홈페이지
    email: '',             // 이메일 (비우면 안 나옴)
    phone: '',             // 전화번호 (비우면 안 나옴)
  },

  /* ── 인쇄(책자) 각주 ─────────────────────────────────────────────────── */
  footer: 'STRIX — LPK SOFT Co., Ltd.',
  // 인쇄물 각 페이지 하단에 들어가는 글자

}
